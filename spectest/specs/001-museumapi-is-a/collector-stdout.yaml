receivers:
  otlp:
    protocols:
      grpc: {}

exporters:
  jaeger:
    endpoint: "http://jaeger:14268/api/traces"
  logging:
    loglevel: debug

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [jaeger, logging]
